{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "gmotion 기본 권한",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-confirm"
  ]
}
